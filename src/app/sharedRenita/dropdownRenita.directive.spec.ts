/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DropdownRenitaDirective } from './dropdownRenita.directive';

describe('Directive: DropdownRenita', () => {
  it('should create an instance', () => {
    const directive = new DropdownRenitaDirective();
    expect(directive).toBeTruthy();
  });
});
